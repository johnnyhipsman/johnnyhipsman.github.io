const { exec } = require('child_process');

exec('java -jar PortfolioWebsite/InventoryManagement.jar', (error, stdout, stderr) => {
    if (error) {
        console.error(`exec error: ${error}`);
        return;
    }
    document.getElementById('output').innerHTML = stdout;
});
